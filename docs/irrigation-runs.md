# Irrigation run plan

The irrigation plan records named zones and their planned run durations.

Operators can reverse the zone sequence when pressure or field access requires starting at the opposite end. Each zone includes its own flow target.

The test renders the complete plan and exercises the reverse-zone interaction.
