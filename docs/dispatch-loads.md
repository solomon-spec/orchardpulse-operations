# Dispatch load sequence

The dispatch sequence shows outbound load numbers and destinations in release order.

Dispatchers can defer the first load when a trailer or seal is delayed. A seal-verification choice belongs to each load.

The focused test covers the initial release order and the first-load deferral action.
